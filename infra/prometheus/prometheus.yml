global:
  scrape_interval: 5s

scrape_configs:
  - job_name: workshop-api
    metrics_path: /metrics
    dns_sd_configs:
      - names: ['api']
        type: A
        port: 3000
        refresh_interval: 5s
