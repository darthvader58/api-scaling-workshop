{
  "name": "api-scaling-workshop",
  "version": "1.0.0",
  "private": true,
  "description": "A no-cloud, Docker-first workshop for learning how to scale an API.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "worker": "node src/worker.js",
    "load": "node loadtest/run.js",
    "format": "node --check src/server.js && node --check src/worker.js && node --check loadtest/run.js"
  },
  "dependencies": {
    "autocannon": "^8.0.0",
    "fastify": "^5.2.1",
    "pg": "^8.13.1",
    "prom-client": "^15.1.3",
    "redis": "^4.7.0"
  }
}
